---
- name: Provision logstash
  hosts:
    - all
  gather_facts: yes
  become: yes
  roles:
    - role: default-packages
    - role: docker-engine
    - role: logstash
    - role: docker_deploy
      docker_apps: "{{ logstash_docker_apps }}"
...
