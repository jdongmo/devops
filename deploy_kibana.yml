---
- name: Provision kibana
  hosts:
    - all
  gather_facts: yes
  become: yes
  roles:
    - role: default-packages
    - role: docker-engine
    - role: kibana
    - role: docker_deploy
      docker_apps: "{{ kibana_docker_apps }}"
...
