---
- name: Provision elasticsearch
  hosts:
    - all
  gather_facts: yes
  become: yes
  roles:
    - role: default-packages
    - role: sysctl
    - role: docker-engine
#    - role: elastic
    - role: docker_deploy
      docker_apps: "{{ elastic_docker_apps }}"
...
